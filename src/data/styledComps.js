import styled from 'styled-components';

//page for syled components to be used universally in the app.

//colored span for highlighting words.
export const Cspan = styled.span`
  color: ${props => props.color};
`;